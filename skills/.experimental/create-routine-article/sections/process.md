## Process

1. Map source pack + routine spec onto the anatomy.
2. Draft per the anatomy, image slots inline as `[IMG-nn — orientation: description]`.
3. Extract the shot list (run history, trigger config, output destination, and the before/after pair are mandatory captures).
4. List gaps, including any spec open items that survived.
5. Run the checklist at the bottom of the anatomy reference.