# Dedicated 1Password template for the __SCRIPT_NAME__ script.
#
# Generate the local env file with:
#   VAULT_NAME=Tux op inject \
#     --in-file=supabase/scripts/.env.1p.__SCRIPT_NAME__ \
#     --out-file=supabase/scripts/.env.__SCRIPT_NAME__ \
#     --force
#
# The generated .env.__SCRIPT_NAME__ file is gitignored. Never commit it.

SUPABASE_URL=op://${VAULT_NAME}/supabase/url
SUPABASE_SERVICE_ROLE_KEY=op://${VAULT_NAME}/supabase/service-role-key
